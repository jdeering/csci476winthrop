#include <string>

#include "Tile.h"

char Tile::getLetter()
{
	return letter;
}

void Tile::toggleImage()
{
	
	//if the letter is not selected, swap out its image for the selected image

	//if the letter is selected, swap out its image for the unselected image

}
